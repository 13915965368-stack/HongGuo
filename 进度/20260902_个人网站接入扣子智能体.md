# 任务：个人网站接入扣子智能体

## 目标

搭建个人网站（React + Vite 前端 + Express 后端代理），通过扣子 API（stream\_run）接入已开发的智能体（图片+文字输出），支持多轮对话。

## 当前状态

- 已完成：官方文档抓取并归档（官方文档/01\~03）

- 已完成：技术选型确认（React+Vite / Express 代理 / 多轮对话 localStorage）

- 已完成：项目骨架搭建（server/ + web/），依赖安装完毕

- 已完成：全链路联调——curl 测试 /api/chat 成功，SSE 正常透传

- 已修复：前端渲染崩溃 Bug——userMsg 缺少 images 字段导致 m.images.length 读取 undefined，React 整树卸载页面空白

- 已验证：浏览器端 UI 全流程——发消息→流式回复渲染正常，Console 无错误

- 已验证：多轮上下文——第二条消息能准确复述第一条消息内容，session\_id 关联有效

- 已修复：回答文本中泄漏 \</think\_xxx> 内部标签的问题（渲染时 sanitizeText 过滤）

- 已验证：图片输出——用真实触发指令（找快递站）测试成功，图片正常渲染。确认格式：智能体在 answer 文本中以 Markdown 图片语法 ![alt](url) 内嵌图片，URL 为 coze-coding-project.tos.coze.site 的对象存储地址

- 已修复：文字气泡中 Markdown 图片源码重复显示问题（sanitizeText 清除 ![alt](url) 源码，图片单独渲染）
- 已重构（P6 设计修正）：删除 extractImages 盲扫 + 图片过滤正则两个 hack，改用 react-markdown + remark-gfm 渲染回复，图片在原文位置就地显示。已实测：图文穿插顺序正确（文字→图→文字）、无 Markdown 源码泄漏、无 think 标签泄漏、图片加载成功。决策记录见 ADR-20260902-Markdown渲染.md

## 下一步

- 功能全部验证完毕，可进入对外发布准备（内网穿透 Cloudflare Tunnel / 域名 / 部署方案）

## 未验证项

- 无（核心链路全部实测通过）

